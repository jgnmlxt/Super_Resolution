function A=slider_crusher(slider,larger_pixel,compressing_factor)

    smaller_pixel=larger_pixel*compressing_factor;
    A=zeros(smaller_pixel^2,(smaller_pixel+compressing_factor-1)^2);
    for i=1:compressing_factor
        for j=1:compressing_factor
            
end
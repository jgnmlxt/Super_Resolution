compressing_factor=5;
l_pixel=10;
shifting_direction='r';

GT=create_image(l_pixel,compressing_factor,0,1);
shifted_image=compress_1D(GT,compressing_factor,l_pixel,shifting_direction);

%% 

final_image=zeros(1,compressing_factor*)